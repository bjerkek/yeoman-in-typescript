{
  "name": "@bjerkek/generator-foo",
  "version": "1.0.2",
  "description": "Generator for Foo",
  "main": "generators/app/index.js",
  "scripts": {
    "build": "rimraf generators && tsc -b && npm run copy-files",
    "watch": "rimraf generators && tsc-watch && npm run copy-files",
    "copy-files": "cp -R ./src/app/filegroups/ ./generators/ && cp -R ./src/app/templates/ ./generators/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Bjerkek AS",
  "license": "ISC",
  "files": [
    "generators/app"
  ],
  "dependencies": {
    "tslib": "2.3.1",
    "typescript": "4.5.4",
    "yeoman-generator": "5.5.2",
    "yosay": "2.0.2"
  },
  "devDependencies": {
    "@types/yeoman-generator": "5.2.8",
    "@types/yosay": "2.0.1",
    "rimraf": "3.0.2"
  }
}
